<%= form_for [:admin, train], html: { style: "#{display_hidden(hidden)}"} do |form| %>
<%= render 'common/errors', resource: train %>
  <div class="form-group">
    <%= form.label :number %>
    <%= form.text_field :number, class: 'form-control' %>
  </div>

  <div class="actions">
    <%= form.submit 'Изменить' %>
  </div>
<% end %>